<!-- Hero Section Component - Recreación exacta -->
<section id="Inicio">
  <div class="max-w-[85rem] mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center min-h-screen py-4 lg:py-8" style="margin-top: -100px;">
      <!-- Content Column -->
      <div class="space-y-8 lg:pr-8">
        <!-- Badge/Tag -->
        <div class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-700">
          Con solo 30 minutos al día
        </div>

        <!-- Main Heading -->
        <div class="space-y-4">
          <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 leading-tight">
            Transformá tu vida
            <br />
            con bienestar
            <br />
            integral
          </h1>
        </div>

        <!-- Description -->
        <p class="text-lg text-gray-600 leading-relaxed max-w-md">
          Planes de ejercicios diseñados con acompañamiento personalizado.
        </p>

        <!-- Button -->
        <div class="pt-2">
          <a 
            href="/contacto"
            class="inline-flex items-center px-6 py-3 bg-green-600 font-bold text-base rounded-md transition-all duration-200 hover:bg-green-700"
          >
            Comenzar ahora
          </a>
        </div>
      </div>

      <!-- Image -->
      <img 
        src="/img/Img.png" 
        alt="Mujer haciendo ejercicio de yoga en casa"
        class="w-full h-full max-h-[800px] object-cover rounded-lg"
      />
    </div>
  </div>
</section>
